<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset Password</title>
    <link rel="stylesheet" href="../../css/form.css">
    <style>
        a{
            text-decoration: none;
            width: fit-content;
        }
    </style>
</head>

<body>
    <div class="container" style="height: 300px;">
        <div class="header">
            <h4> <%= !submit ? "Find your account..." : "Password reset Link has been mailed." %></h4>
        </div>
        <% if(!submit) { %>
        <div class="body">
            <form class="userForm" action="/user/forgotPass" method="post">
                <div class="form-group">
                    <label for="name">Email Id or Username</label>
                    <input class="input-field" type="text" name="userID" id="userID" autocomplete="off" placeholder="Email or Username">
                </div>
                <div class="form-group">
                    <input class="btn" type="submit" value="Find my account">
                </div>
            </form>
        </div>
        <% } else { %> 
        <div class="body"style="align-items:center;justify-content : space-around">
                <a href="/user/login"><input type="button" class="btn" value="Back to login"></a>
        </div>
        <% } %> 
    </div>
</body>

</html>